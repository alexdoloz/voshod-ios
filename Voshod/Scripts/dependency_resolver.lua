local function DependencyResolver(specifiers, dependencies)
    function resolver:resolveSpecifierPattern(specifierPattern) 
        
    end

    function resolver:resolveSpecifier(specifier)

    end
end

return DependencyResolver